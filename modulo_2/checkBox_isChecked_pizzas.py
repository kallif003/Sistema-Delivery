def isChecked(tela, select):

    telaPrincipal = tela
    dados = select
    listaTemp = []
    valorProduto = []

    if telaPrincipal.checkBox1.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[0])
            valorProduto.append(dados[0][2])
            break
    if telaPrincipal.checkBox2.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[1])
            valorProduto.append(dados[1][2])
            break
    if telaPrincipal.checkBox3.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[2])
            valorProduto.append(dados[2][2])
            break
    if telaPrincipal.checkBox4.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[3])
            valorProduto.append(dados[3][2])
            break
    if telaPrincipal.checkBox5.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[4])
            valorProduto.append(dados[4][2])
            break
    if telaPrincipal.checkBox6.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[5])
            valorProduto.append(dados[5][2])
            break
    if telaPrincipal.checkBox7.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[6])
            valorProduto.append(dados[6][2])
            break
    if telaPrincipal.checkBox8.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[7])
            valorProduto.append(dados[7][2])
            break
    if telaPrincipal.checkBox9.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[8])
            valorProduto.append(dados[8][2])
            break
    if telaPrincipal.checkBox10.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[9])
            valorProduto.append(dados[9][2])
            break
    if telaPrincipal.checkBox11.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[10])
            valorProduto.append(dados[10][2])
            break
    if telaPrincipal.checkBox12.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[11])
            valorProduto.append(dados[11][2])
            break
    if telaPrincipal.checkBox13.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[12])
            valorProduto.append(dados[12][2])
            break
    if telaPrincipal.checkBox14.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[13])
            valorProduto.append(dados[13][2])
            break
    if telaPrincipal.checkBox15.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[14])
            valorProduto.append(dados[14][2])
            break
    if telaPrincipal.checkBox16.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[15])
            valorProduto.append(dados[15][2])
            break
    if telaPrincipal.checkBox17.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[16])
            valorProduto.append(dados[16][2])
            break
    if telaPrincipal.checkBox18.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[17])
            valorProduto.append(dados[17][2])
            break
    if telaPrincipal.checkBox19.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[18])
            valorProduto.append(dados[18][2])
            break
    if telaPrincipal.checkBox20.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[19])
            valorProduto.append(dados[19][2])
            break
    if telaPrincipal.checkBox21.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[20])
            valorProduto.append(dados[20][2])
            break
    if telaPrincipal.checkBox22.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[21])
            valorProduto.append(dados[21][2])
            break
    if telaPrincipal.checkBox23.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[22])
            valorProduto.append(dados[22][2])
            break
    if telaPrincipal.checkBox24.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[23])
            valorProduto.append(dados[23][2])
            break
    if telaPrincipal.checkBox25.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[24])
            valorProduto.append(dados[24][2])
            break
    if telaPrincipal.checkBox26.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[25])
            valorProduto.append(dados[25][2])
            break
    if telaPrincipal.checkBox27.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[26])
            valorProduto.append(dados[26][2])
            break
    if telaPrincipal.checkBox28.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[27])
            valorProduto.append(dados[27][2])
            break
    if telaPrincipal.checkBox29.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[28])
            valorProduto.append(dados[28][2])
            break
    if telaPrincipal.checkBox30.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[29])
            valorProduto.append(dados[29][2])
            break
    if telaPrincipal.checkBox31.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[30])
            valorProduto.append(dados[30][2])
            break
    if telaPrincipal.checkBox32.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[31])
            valorProduto.append(dados[31][2])
            break
    if telaPrincipal.checkBox33.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[32])
            valorProduto.append(dados[32][2])
            break
    if telaPrincipal.checkBox34.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[33])
            valorProduto.append(dados[33][2])
            break
    if telaPrincipal.checkBox35.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[34])
            valorProduto.append(dados[34][2])
            break
    if telaPrincipal.checkBox36.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[35])
            valorProduto.append(dados[35][2])
            break
    if telaPrincipal.checkBox37.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[36])
            valorProduto.append(dados[36][2])
            break
    if telaPrincipal.checkBox38.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[37])
            valorProduto.append(dados[37][2])
            break
    if telaPrincipal.checkBox39.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[38])
            valorProduto.append(dados[38][2])
            break
    if telaPrincipal.checkBox40.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[39])
            valorProduto.append(dados[39][2])
            break
    if telaPrincipal.checkBox41.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[40])
            valorProduto.append(dados[40][2])
            break
    if telaPrincipal.checkBox42.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[41])
            valorProduto.append(dados[41][2])
            break
    if telaPrincipal.checkBox43.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[42])
            valorProduto.append(dados[42][2])
            break
    if telaPrincipal.checkBox44.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[43])
            valorProduto.append(dados[43][2])
            break
    if telaPrincipal.checkBox45.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[44])
            valorProduto.append(dados[44][2])
            break
    if telaPrincipal.checkBox46.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[45])
            valorProduto.append(dados[45][2])
            break
    if telaPrincipal.checkBox47.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[46])
            valorProduto.append(dados[46][2])
            break
    if telaPrincipal.checkBox48.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[47])
            valorProduto.append(dados[47][2])
            break
    if telaPrincipal.checkBox49.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[48])
            valorProduto.append(dados[48][2])
            break
    if telaPrincipal.checkBox50.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[49])
            valorProduto.append(dados[49][2])
            break
    if telaPrincipal.checkBox51.isChecked():
        for i in range(len(dados)):
            listaTemp.append(dados[50])
            valorProduto.append(dados[50][2])
            break

    return listaTemp, valorProduto
